# Быстрый старт - Функция Дуа

## 1. Установка (5 минут)

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Настройте .env файл
cp .env.example .env
```

Отредактируйте `.env`:
```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_PASSWORD=72780407
ADMIN_CHAT_ID=ваш_telegram_id
```

## 2. Запуск

```bash
python main.py
```

## 3. Тестирование

### В Telegram:

1. Найдите вашего бота
2. Отправьте `/start`
3. Нажмите кнопку "Оставить дуа"
4. Выберите имя (реальное или анонимно)
5. Введите текст дуа
6. Получите подтверждение

### Полезные команды:

- `/start` - Регистрация и главное меню
- `/menu` - Показать меню
- `/stats` - Статистика дуа
- `/help` - Справка

## 4. Что реализовано

✅ **База данных**
- Таблица `users` с языками и состояниями
- Таблица `duas` с лимитами по Жума

✅ **FSM состояния**
- `WAITING_DUA_NAME_CHOICE` - выбор имени
- `WAITING_DUA` - ввод текста дуа

✅ **Лимиты**
- 2 дуа на пользователя за Жума
- 20 дуа всего за Жума
- Предупреждение когда остается < 5 мест

✅ **Многоязычность**
- Русский
- O'zbekcha (латиница)
- O'zbekcha (кириллица)

✅ **Отправка администратору**
- Дуа автоматически отправляются в чат админа
- С информацией об отправителе

## 5. Структура файлов

```
bot/
├── config.py              # Настройки (токен, лимиты)
├── states.py              # FSM состояния
├── middlewares.py         # Middleware для БД
├── database/
│   └── models.py          # Модели БД и методы для дуа
├── handlers/
│   ├── dua_handlers.py    # Основные обработчики дуа
│   └── test_handlers.py   # Тестовые команды
└── locales/
    └── texts.py           # Тексты на 3 языках
```

## 6. Примеры использования

### Отправка первой дуа:
1. `/start` → Главное меню
2. Нажать "Оставить дуа"
3. Выбрать "Моё имя"
4. Ввести: "Пусть мои родители будут здоровы"
5. ✅ Дуа отправлена!

### Проверка лимитов:
```bash
# В Telegram
/stats

# Вы увидите:
# Ваши дуа на эту Жума: 1/2
# Всего дуа на эту Жума: 1/20
```

## 7. Что делать дальше

Для полноценной работы бота нужно реализовать:
- [ ] Полную регистрацию пользователей
- [ ] Выбор языка при первом запуске
- [ ] Марафон садака
- [ ] Календарь выполнения
- [ ] Напоминания
- [ ] Админ-панель

Но функция дуа уже полностью рабочая! 🎉

## 8. Решение проблем

**Бот не запускается:**
- Проверьте BOT_TOKEN в .env
- Убедитесь что установлены все зависимости

**Дуа не приходит админу:**
- Проверьте ADMIN_CHAT_ID в .env
- Убедитесь что бот может отправлять сообщения

**База данных не создается:**
- Проверьте права на запись в папке проекта
- Файл `sadaka_bot.db` должен создаться автоматически

## 9. Документация

Подробная документация:
- `README.md` - Общее описание
- `DUA_FLOW.md` - Схема работы функции дуа
- `TESTING_GUIDE.md` - Полное руководство по тестированию
